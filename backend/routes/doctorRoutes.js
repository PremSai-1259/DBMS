const express = require('express');
const router = express.Router();

// Note: Doctor approval routes are in doctorApprovalsRoutes.js
// This file is currently empty - add doctor-specific endpoints here if needed

module.exports = router;
